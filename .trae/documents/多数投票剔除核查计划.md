# 核查多数投票剔除的文献

核查刚刚被 Majority Voting (n=3) 策略剔除的 12 篇文献。

## 1. 策略
*   由于我刚刚覆盖了 `Literature_Screening_List.xlsx`，我需要对比 `Literature_Screening_List（1）.xlsx` (原始全集) 和当前文件，或者对比 Git 上一个版本。
*   **最佳方案**: 直接解析 `robust_screening_voting.py` 的日志输出（最快）。
*   **替代方案**: 编写脚本，读取 `Literature_Screening_List（1）.xlsx` 中存在但 `Literature_Screening_List.xlsx` 中不存在的 PMID。

## 2. 编写 `check_voting_results.py`
*   读取 `Literature_Screening_List（1）.xlsx` (作为 source_df)。
*   读取 `Literature_Screening_List.xlsx` (作为 target_df)。
*   找出 source_df 中存在但 target_df 中不存在的行。
*   打印这些行的标题。

## 3. 执行
*   运行脚本。
*   输出列表供用户审核。
