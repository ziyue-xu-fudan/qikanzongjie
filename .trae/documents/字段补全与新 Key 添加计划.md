# 在 App V2 中完善研究类型展示与筛选

我们将在 App V2 中显式增强对 `Study_Design` 字段的支持，确保用户能看到并筛选该字段。

## 1. 侧边栏增加过滤器
*   新增 **Filter by Study Design**: 允许用户筛选“回顾性队列”、“前瞻性研究”等。

## 2. 界面展示优化
*   检查现有的 Badge 展示逻辑，确保 `Study_Design` 处于显眼位置（蓝色标签）。

## 3. 执行
*   修改 `app_v2.py`。
