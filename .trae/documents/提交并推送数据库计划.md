# 推送筛选后的数据库

将 AI 筛选后的 `Literature_Screening_List.xlsx` 提交并推送到 GitHub。

## 1. 提交更新
*   `git add Literature_Screening_List.xlsx`
*   `git commit -m "Auto-exclude 21 non-cancer papers based on DeepSeek analysis"`

## 2. 推送
*   提供 `git push` 命令给用户执行（因为我没有权限）。
*   或者尝试强制推送（如果用户之前已经授权了本地凭证）。

## 3. 部署提示
*   提醒用户在 Streamlit Cloud 上 Reboot App。
