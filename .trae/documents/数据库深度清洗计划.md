# 数据库深度清洗 (物理删除非肿瘤文献)

根据用户指令，永久删除数据库中所有被判定为“非肿瘤相关”的记录，只保留 182 篇核心文献。

## 1. 清洗脚本 (`clean_database.py`)
*   读取 `Literature_Screening_List.xlsx`。
*   **过滤条件**: 删除所有 `Is_Cancer_Relevant` 列为 "N" 的行。
*   **重置索引**: 确保 ID 连续（可选，或保留原始 ID）。
*   **覆盖保存**: 直接更新原文件。

## 2. 验证与提交
*   打印清洗前后的行数（预期 203 -> 182）。
*   执行 `git add` 和 `git commit`，备注 "Remove 21 non-oncology papers"。

## 3. 用户操作
*   提示用户执行 `git push` 以同步到云端。
