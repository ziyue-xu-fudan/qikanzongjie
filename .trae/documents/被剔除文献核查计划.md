# 核查被剔除的文献

通过 Git 历史找回被剔除的 38 篇（第一轮 21 + 第二轮 17）文献，并打印其标题和剔除理由供用户核查。

## 1. 策略
*   不直接回退 Git（以免扰乱当前工作区），而是使用 `git show` 命令读取旧版本的 Excel 文件内容。
*   或者更简单：直接解析之前的终端日志（如果有保留）。
*   **最佳方案**: 既然之前的 Excel 已经 commit 过了（在清洗前），我们可以用 `git checkout HEAD~2 Literature_Screening_List.xlsx` 把旧文件拿回来暂存，读取完再还原。

## 2. 编写脚本 `check_excluded_papers.py`
*   将当前 Excel 备份为 `temp_current.xlsx`。
*   执行 `git checkout HEAD~2 Literature_Screening_List.xlsx` (回退到清洗前)。
*   读取 Excel，筛选 `Is_Cancer_Relevant == 'N'` 的行。
*   打印标题和理由。
*   恢复：`mv temp_current.xlsx Literature_Screening_List.xlsx`。

## 3. 执行
*   运行脚本，输出列表。
